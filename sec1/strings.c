#include <stdio.h>

int main(){
    char *word = "Hello World!";
    printf("word = %s\n", word);
    
    // char *x = word + 1;
    // printf("x = %s\n", x);

    // char *y = (char *)(((int *)word)+ 1);
    // printf("y = %s\n", y);
    return 0;
}
